{
  "extends": "next/core-web-vitals",  // Extends Next.js' default ESLint configuration for core web vitals
  "rules": {
    "react/no-unescaped-entities": "off"  // Disables the rule for unescaped HTML entities in JSX
  }
}